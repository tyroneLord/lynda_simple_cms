= link_to "<< Back to List" ,  "/" , :class => 'back-link'

.subject.delete
  %h2 Delete Subject
  = form_for :subject, :url => {:action => 'destroy', :id => @subject.id} do |f|
    %p Do you want to delete
    %p.reference-name=@subject.name
    .form-buttons
      = submit_tag "Delete Subject"
